@page
@model ASP_NET_INMN2_PA2.Pages.Miasta.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Miasta[0].Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miasta[0].Powierzchnia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miasta[0].Populacja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miasta[0].DataZałożenia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miasta[0].Województwo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Miasta[0].Państwo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Miasta) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Powierzchnia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Populacja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataZałożenia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Województwo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Państwo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
